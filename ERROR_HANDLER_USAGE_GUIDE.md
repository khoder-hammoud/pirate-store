# 📖 دليل استخدام errorHandlerUtil - Usage Guide\n\n## 🎯 الغرض\n\nمعالج مركزي للأخطاء يحول error objects إلى HTTP status codes و messages.\n\n---\n\n## 💻 الاستخدام الأساسي\n\n### الخطوة 1: الاستيراد\n```javascript\nconst { handleOrderError, handleRefundError } = require('../middleware/errorHandlerUtil');\n```\n\n### الخطوة 2: الاستخدام في Catch\n```javascript\ncatch (error) {\n  console.error('Error:', error);\n  const { status, message } = handleOrderError(error);\n  res.status(status).json({ message });\n}\n```\n\n---\n\n## 📚 الأخطاء المدعومة\n\n### handleOrderError() - لأخطاء الطلبات\n\n| Error Message | HTTP Status | Description |\n|-------------|------------|-------------|\n| `Not authorized` | 403 | المستخدم غير مصرح |\n| `Order not found` | 404 | الطلب غير موجود |\n| `Order already paid` | 400 | الطلب مدفوع بالفعل |\n| `Order not paid yet` | 400 | الطلب لم يتم دفعه |\n| `Order already delivered` | 400 | الطلب تم توصيله بالفعل |\n| `Order already cancelled` | 400 | الطلب ملغاه بالفعل |\n| `Insufficient stock` | 400 | المخزون غير كافي |\n| `Cancel window expired` | 400 | انتهت فترة الإلغاء |\n| Any `Invalid status` error | 400 | خطأ في الحالة |\n\n### handleRefundError() - لأخطاء ال Refunds\n\n| Error Message | HTTP Status | Description |\n|-------------|------------|-------------|\n| `Order not found` | 404 | الطلب غير موجود |\n| `Not authorized` | 403 | غير مصرح |\n| `No refund found` | 404 | لا يوجد refund |\n| `Amount must be positive` | 400 | المبلغ سالب |\n| Contains `already exists` | 400 | Refund موجود بالفعل |\n| Any unknown error | 500 | خطأ داخلي |\n\n---\n\n## 🔄 أمثلة عملية\n\n### مثال 1: تحديث الطلب للدفع\n```javascript\nconst updateOrderToPaid = async (req, res) => {\n  try {\n    const order = await orderService.updateOrderToPaid(req.params.id, req.user);\n    res.status(200).json({ message: 'Order paid', order });\n  } catch (error) {\n    console.error('Update Order To Paid Error:', error);\n    \n    // ✅ معالجة موحدة\n    const { status, message } = handleOrderError(error);\n    res.status(status).json({ message });\n  }\n};\n```\n\n**الأخطاء المتوقعة:**\n```\nthrow new Error('Not authorized');              → status 403\nthrow new Error('Order not found');             → status 404\nthrow new Error('Order already paid');          → status 400\nthrow new Error('Insufficient stock');          → status 400\nthrow new Error('Invalid status transition');   → status 400\n```\n\n---\n\n### مثال 2: إلغاء الطلب\n```javascript\nconst cancelOrder = async (req, res) => {\n  try {\n    const order = await orderService.cancelOrder(req.params.id, req.user);\n    res.status(200).json({ message: 'Order cancelled', order });\n  } catch (error) {\n    console.error('Cancel Order Error:', error);\n    \n    // ✅ معالجة موحدة\n    const { status, message } = handleOrderError(error);\n    res.status(status).json({ message });\n  }\n};\n```\n\n**الأخطاء المتوقعة:**\n```\nthrow new Error('Not authorized');              → status 403\nthrow new Error('Order not found');             → status 404\nthrow new Error('Order already cancelled');     → status 400\nthrow new Error('Cancel window expired');       → status 400\nthrow new Error('Invalid status transition');   → status 400\n```\n\n---\n\n### مثال 3: Refund\n```javascript\nconst initiateRefund = async (req, res) => {\n  try {\n    const { id: orderId } = req.params;\n    const { amount } = req.body;\n    \n    await refundService.validateRefundRequest(orderId, amount, ...);\n    const refund = await refundService.createRefund({ orderId, amount });\n    \n    res.status(201).json({ message: 'Refund initiated', refund });\n  } catch (error) {\n    console.error('Initiate Refund Error:', error);\n    \n    // ✅ معالجة موحدة\n    const { status, message } = handleRefundError(error);\n    res.status(status).json({ message });\n  }\n};\n```\n\n**الأخطاء المتوقعة:**\n```\nthrow new Error('Order not found');             → status 404\nthrow new Error('Not authorized');              → status 403\nthrow new Error('Amount must be positive');     → status 400\n```\n\n---\n\n## 🛠️ كيفية إضافة أخطاء جديدة\n\n### خطوة 1: أضف الخطأ في Service\n```javascript\n// في orderService.js\nif (someCondition) {\n  throw new Error('My new error message');\n}\n```\n\n### خطوة 2: أضفها في errorHandlerUtil\n```javascript\n// في middleware/errorHandlerUtil.js\nconst handleOrderError = (error) => {\n  const errorResponses = {\n    'My new error message': { status: 400, message: error.message },\n    // ... باقي الأخطاء\n  };\n  // ...\n};\n```\n\n### خطوة 3: استخدمها في Controller\n```javascript\ncatch (error) {\n  const { status, message } = handleOrderError(error);\n  res.status(status).json({ message });\n}\n```\n\n---\n\n## 🔍 Debugging\n\n### إذا لم يتم التعرف على الخطأ\n```javascript\n// الخطأ سيعود status 500 بشكل افتراضي\nif (!errorResponses[error.message]) {\n  return { status: 500, message: 'Internal Server Error' };\n}\n```\n\n### فحص الأخطاء المدعومة\n```javascript\n// في terminal:\ngrep -n \"'\" middleware/errorHandlerUtil.js | grep \":\"\n```\n\n---\n\n## 📊 الفوائد\n\n| الفائدة | التفسير |\n|--------|--------|\n| **DRY** | لا تكرار منطق معالجة الأخطاء |\n| **Centralized** | موقع واحد لتعديل معالجة الأخطاء |\n| **Consistent** | نفس status codes في كل مكان |\n| **Maintainable** | سهل الإضافة والتعديل |\n| **Testable** | سهل كتابة unit tests |\n\n---\n\n## ✅ Best Practices\n\n1. **اجعل الرسائل واضحة**\n   ```javascript\n   ✅ throw new Error('Order already paid');\n   ❌ throw new Error('ERR_INVALID_ORDER_STATUS');\n   ```\n\n2. **اجعل الرسائل متسقة**\n   ```javascript\n   ✅ Use the same messages everywhere\n   ❌ 'Not authorized' vs 'Unauthorized'\n   ```\n\n3. **استخدم المعالج دائماً**\n   ```javascript\n   ✅const { status, message } = handleOrderError(error);\n   ❌ res.status(400).json({ message: error.message });\n   ```\n\n4. **سجل الأخطاء قبل الرد**\n   ```javascript\n   ✅console.error('Error details:', error);\n   ✅const { status, message } = handleOrderError(error);\n   ❌skip logging\n   ```\n\n---\n\n## 🎯 ملخص\n\n| ما قبل | ما بعد |\n|--------|--------|\n| معالجة أخطاء متفرقة | معالجة مركزية ✅ |\n| كود مكرر | كود موحد ✅ |\n| صعوبة الصيانة | سهولة الصيانة ✅ |\n| غير متسق | متسق ✅ |\n\n---\n\n**جاهز للاستخدام:** ✅  \n**الإصدار:** 1.0  \n**آخر تحديث:** مايو 2026\n